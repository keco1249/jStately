project.name			=jStately

# Top-level directories
project.home.dir		=${basedir}
project.src.dir			=${basedir}/src
project.lib.dir			=${basedir}/lib
project.lib.runtime.dir	=${project.lib.dir}/runtime
project.build.dir		=${basedir}/build
project.target.dir		=${basedir}/target

# Source directories
project.src.java.dir	=${project.src.dir}/java
project.src.test.dir	=${project.src.dir}/test

# Test directories
project.test.report.dir	=${project.target.dir}/junit
project.test.output.dir	=${project.test.report.dir}/output
project.test.html.dir	=${project.test.report.dir}/report

#-----------------------------------------------------------------------------
# Lib target properties
#-----------------------------------------------------------------------------
jar.filename			=${project.name}.jar
jar.filepath			=${project.target.dir}/${jar.filename}


